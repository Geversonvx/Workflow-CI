name: mlflow-env

channels:
  - conda-forge

dependencies:
  - python=3.10
  - pip
  - pip:
      - mlflow==2.10.2
      - cloudpickle
      - numpy
      - pandas
      - scikit-learn
      - scipy
